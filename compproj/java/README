usage:
1) make
2) ./min-ml program.ml, where program.ml is a min-ml program
  (for instance,  ./min-ml ../minml/fib.ml).

NOTES:
Cup parser generator documentation is available there:
http://www2.cs.tum.edu/projects/cup/

Java >= 1.7 is required. On mandelbrot, make sure jdk7 is used by 
setting your PATH correctly: 

export PATH=/usr/lib/jvm/java-7-openjdk-amd64/bin/:$PATH


